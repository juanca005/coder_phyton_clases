#Muestro el menu

#defino una condicion para que el sistema continue en ejecución
x = True
while x:
    #Muestro el menu al usuario
    option = int(input ("Ingrese una opción:\n 1 - Sumar dos numeros \n 2 - Restar dos numeros \n 3 - Multiplicar dos numeros \n 4 - Salir \n"))
    #Calculo la suma
    if option == 1:

        num_1 = int(input("Ingrese el primer numero:\n"))
        num_2 = int(input("Ingrese el segundo numero:\n"))

        total= num_1+num_2
        print("el resultado es", total)
    #Calculo la resta
    elif option == 2:

        num_1 = int(input("Ingrese el primer numero:\n"))
        num_2 = int(input("Ingrese el segundo numero:\n"))

        total= num_1-num_2
        print("el resultado es", total)
    #Calculo la multiplicación
    elif option == 3:

        num_1 = int(input("Ingrese el primer numero:\n"))
        num_2 = int(input("Ingrese el segundo numero:\n"))

        total= num_1*num_2
        print("el resultado es:\n", total)
    #Cambio la condicion a False para que deje de ejecutarse el while
    elif option == 4:
        print("Gracias por utilizar el sistema:\n")
        x = False
    #cualquier opcion indico que no es valida para que siga ejecutandose
    else:
        print("Seleccione una opción valida:\n")
        continue
